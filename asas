local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Camera = workspace.CurrentCamera
local LocalPlayer = Players.LocalPlayer

-- === CONFIGURATION ===
local espInventoryEnabled = true 
local MAX_DISTANCE = 1500 
local BillboardCache = {}
local nameCache = {} 

local RARITY_COLORS = {
    ["Common"] = Color3.fromRGB(255, 255, 255),
    ["Uncommon"] = Color3.fromRGB(99, 255, 52),
    ["Rare"] = Color3.fromRGB(51, 170, 255),
    ["Epic"] = Color3.fromRGB(237, 44, 255),
    ["Legendary"] = Color3.fromRGB(255, 150, 0),
    ["Omega"] = Color3.fromRGB(255, 20, 51),
    ["Mythic"] = Color3.fromRGB(255, 0, 0),
}

-- === LE NETTOYEUR DE NOM RADICAL ===
local function getRealName(tool)
    if not tool or not tool.Name then return nil end
    
    -- 1. On cherche d'abord un nom d'affichage propre dans les réglages du jeu
    local displayName = tool:GetAttribute("ItemName") or tool:GetAttribute("DisplayName")
    if displayName and tostring(displayName) ~= "" then
        return tostring(displayName)
    end

    -- 2. SUPPRESSION TOTALE DES IDS (On ne garde que les lettres A-Z)
    -- Cette ligne retire TOUS les chiffres et symboles
    local cleanName = tool.Name:gsub("[%d%p%c%s]+", " "):match("^%s*(.-)%s*$")
    
    -- Si le nom devient vide, on remet le nom original sans les chiffres
    if not cleanName or #cleanName < 1 then
        cleanName = tool.Name:gsub("%d+", "")
    end
    
    return cleanName
end

-- === DÉTECTION DE LA COULEUR (FIX) ===
local function getRarityColor(tool)
    -- On cherche la rareté partout (Attributs ou Valeurs internes)
    local rarity = tool:GetAttribute("Rarity") or tool:GetAttribute("RarityName")
    
    if not rarity then
        local folder = tool:FindFirstChild("Rarity") or tool:FindFirstChild("Settings")
        if folder and folder:FindFirstChild("Rarity") then
            rarity = folder.Rarity.Value
        end
    end
    
    return RARITY_COLORS[tostring(rarity)] or Color3.new(1, 1, 1) -- Blanc si non trouvé
end

local function updateESP(p)
    local bb = BillboardCache[p]
    if not bb or not espInventoryEnabled then 
        if bb then bb.Enabled = false end
        return 
    end
    
    local char = p.Character
    if not char or not char:FindFirstChild("HumanoidRootPart") then 
        bb.Enabled = false
        return 
    end
    
    local container = bb:FindFirstChild("EspContainer")
    if not container then return end
    container:ClearAllChildren()

    local layout = Instance.new("UIListLayout", container)
    layout.HorizontalAlignment = Enum.HorizontalAlignment.Center
    layout.VerticalAlignment = Enum.VerticalAlignment.Bottom 

    local tools = {}
    for _, t in ipairs(p.Backpack:GetChildren()) do
        if t:IsA("Tool") and t.Name:lower() ~= "fists" then table.insert(tools, t) end
    end
    for _, t in ipairs(char:GetChildren()) do
        if t:IsA("Tool") and t.Name:lower() ~= "fists" then table.insert(tools, t) end
    end

    for _, tool in ipairs(tools) do
        local name = getRealName(tool)
        local color = getRarityColor(tool)
        
        local lbl = Instance.new("TextLabel", container)
        lbl.Size = UDim2.new(0, 200, 0, 16)
        lbl.BackgroundTransparency = 1
        lbl.Text = name
        lbl.TextColor3 = color -- Applique enfin la couleur !
        lbl.Font = Enum.Font.SourceSansBold
        lbl.TextSize = 14
        lbl.TextStrokeTransparency = 0.2
    end
    
    bb.Enabled = (#tools > 0)
end

-- === INITIALISATION ===
local function createESP(p)
    if p == LocalPlayer then return end
    local function setup(char)
        local root = char:WaitForChild("HumanoidRootPart", 10)
        if not root then return end
        if BillboardCache[p] then BillboardCache[p]:Destroy() end
        
        local bb = Instance.new("BillboardGui", root)
        bb.Name = "InventoryFix"
        bb.Size = UDim2.new(0, 200, 0, 100)
        bb.StudsOffset = Vector3.new(0, -4, 0) -- Sous les pieds pour pas gêner
        bb.AlwaysOnTop = true
        
        local cont = Instance.new("Frame", bb)
        cont.Name = "EspContainer"
        cont.Size = UDim2.new(1, 0, 1, 0)
        cont.BackgroundTransparency = 1
        BillboardCache[p] = bb
    end
    p.CharacterAdded:Connect(setup)
    if p.Character then setup(p.Character) end
end

-- Boucle 1 seconde (Zéro Lag)
task.spawn(function()
    while true do
        for _, p in ipairs(Players:GetPlayers()) do
            if p ~= LocalPlayer then updateESP(p) end
        end
        task.wait(1)
    end
end)

for _, p in ipairs(Players:GetPlayers()) do createESP(p) end
Players.PlayerAdded:Connect(createESP)
