--// SERVICES
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer

--// CONFIGURATION
local espInventoryEnabled = true 
local MAX_DISTANCE = 2000 
local UPDATE_INTERVAL = 1.5 
local BillboardCache = {}
local nameCache = {} 

local RARITY_COLORS = {
    ["Common"] = Color3.fromRGB(255, 255, 255),
    ["Uncommon"] = Color3.fromRGB(99, 255, 52),
    ["Rare"] = Color3.fromRGB(51, 170, 255),
    ["Epic"] = Color3.fromRGB(237, 44, 255),
    ["Legendary"] = Color3.fromRGB(255, 150, 0),
    ["Omega"] = Color3.fromRGB(255, 20, 51)
}

--// FILTRE ANTI-ID & NETTOYAGE [cite: 2, 3]
local function getRealName(t)
    if not t or not t.Name then return nil end
    if nameCache[t.Name] then return nameCache[t.Name] end
    
    -- Si le nom est juste un long chiffre (ID), on ignore pour les FPS
    if t.Name:match("^%d+$") and #t.Name > 5 then return nil end
    
    local clean = t.Name:gsub("%d+", ""):gsub("%s+", " "):match("^%s*(.-)%s*$")
    local finalName = (clean ~= "" and clean ~= "Tool") and clean or t.Name
    
    nameCache[t.Name] = finalName
    return finalName
end

--// LOGIQUE SPECIALE BLOCKSPIN (RARETÉ) [cite: 9, 10]
local function getToolRarity(tool)
    local rarity = tool:GetAttribute("RarityName") or tool:GetAttribute("Rarity")
    if rarity then return rarity end
    
    -- Recherche dans ReplicatedStorage si l'attribut manque 
    local itemsFolder = ReplicatedStorage:FindFirstChild("Items")
    if itemsFolder then
        local found = itemsFolder:FindFirstChild(tool.Name, true)
        if found then
            return found:GetAttribute("RarityName") or found:GetAttribute("Rarity") or "Common"
        end
    end
    return "Common"
end

--// MISE À JOUR ESP [cite: 8, 9]
local function updateESP(p)
    local bb = BillboardCache[p]
    if not bb then return end
    
    local char = p.Character
    if not char or not char:FindFirstChild("HumanoidRootPart") then 
        bb.Enabled = false
        return 
    end
    
    local container = bb:FindFirstChild("EspContainer")
    if not container then return end
    container:ClearAllChildren()

    local layout = Instance.new("UIListLayout", container)
    layout.HorizontalAlignment = Enum.HorizontalAlignment.Center
    layout.VerticalAlignment = Enum.VerticalAlignment.Bottom 
    layout.Padding = UDim.new(0, 2)

    local tools = {}
    local function collect(folder)
        if not folder then return end
        for _, t in ipairs(folder:GetChildren()) do
            if t:IsA("Tool") and t.Name:lower() ~= "fists" then
                table.insert(tools, t)
            end
        end
    end

    collect(p:FindFirstChild("Backpack"))
    collect(char)

    bb.Enabled = espInventoryEnabled and (#tools > 0)
    
    for _, tool in ipairs(tools) do
        local name = getRealName(tool)
        if name then
            local rarity = getToolRarity(tool)
            if RARITY_COLORS[rarity] then
                local lbl = Instance.new("TextLabel", container)
                lbl.Size = UDim2.new(0, 180, 0, 14)
                lbl.BackgroundTransparency = 1
                lbl.Text = name
                lbl.Font = Enum.Font.SourceSansBold
                lbl.TextSize = 11
                lbl.TextColor3 = RARITY_COLORS[rarity]
                lbl.TextStrokeTransparency = 0.4
            end
        end
    end
end

--// CREATION GUI [cite: 12, 13]
local function createESP(p)
    if p == LocalPlayer then return end
    local function setup(char)
        local root = char:WaitForChild("HumanoidRootPart", 10)
        if not root then return end
        if BillboardCache[p] then BillboardCache[p]:Destroy() end
        
        local bb = Instance.new("BillboardGui", root)
        bb.Name = "Anouar_Blockspin_ESP"
        bb.Size = UDim2.new(0, 200, 0, 150)
        bb.StudsOffset = Vector3.new(0, -3.5, 0) 
        bb.AlwaysOnTop = true
        bb.MaxDistance = MAX_DISTANCE
        
        local cont = Instance.new("Frame", bb)
        cont.Name = "EspContainer"
        cont.Size = UDim2.new(1, 0, 1, 0)
        cont.BackgroundTransparency = 1
        
        BillboardCache[p] = bb
        updateESP(p)
    end
    p.CharacterAdded:Connect(setup)
    if p.Character then setup(p.Character) end
end

--// INIT
for _, p in ipairs(Players:GetPlayers()) do createESP(p) end
Players.PlayerAdded:Connect(createESP)

task.spawn(function()
    while true do
        for _, p in ipairs(Players:GetPlayers()) do
            if p ~= LocalPlayer then updateESP(p) end
        end
        task.wait(UPDATE_INTERVAL)
    end
end)

--// MINI MENU [cite: 17, 18]
local ScreenGui = Instance.new("ScreenGui", game.CoreGui)
local Main = Instance.new("Frame", ScreenGui)
Main.Size = UDim2.new(0, 200, 0, 90)
Main.Position = UDim2.new(0.5, -100, 0.02, 0)
Main.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
Instance.new("UICorner", Main)

local Title = Instance.new("TextLabel", Main)
Title.Size = UDim2.new(1, 0, 0, 30)
Title.BackgroundColor3 = Color3.fromRGB(170, 0, 0)
Title.Text = "ANOUAR - BLOCKSPIN"
Title.TextColor3 = Color3.new(1, 1, 1)
Title.Font = Enum.Font.GothamBold
Instance.new("UICorner", Title)

local Btn = Instance.new("TextButton", Main)
Btn.Size = UDim2.new(0.9, 0, 0, 40)
Btn.Position = UDim2.new(0.05, 0, 0.45, 0)
Btn.BackgroundColor3 = Color3.fromRGB(0, 180, 0)
Btn.Text = "ESP Inventory: ON"
Btn.TextColor3 = Color3.new(1, 1, 1)
Instance.new("UICorner", Btn)

Btn.MouseButton1Click:Connect(function()
    espInventoryEnabled = not espInventoryEnabled
    Btn.Text = "ESP Inventory: " .. (espInventoryEnabled and "ON" or "OFF")
    Btn.BackgroundColor3 = espInventoryEnabled and Color3.fromRGB(0, 180, 0) or Color3.fromRGB(180, 0, 0)
end)
